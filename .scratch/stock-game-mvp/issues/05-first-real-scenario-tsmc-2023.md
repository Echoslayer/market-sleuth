# 05 — 第一關真實內容上線：2330 台積電 2023/04～2023/07

**What to build:** 用真實 yfinance 股價資料，加上人工蒐集、策展的真實新聞內容（標題、內文、重要度評分、事件時間軸摘要），跑過 03 的 pipeline 產出真正的 `scenario.json`，接進 02 的前端，完整玩過這一關。不依賴 04 的 LLM 草稿步驟即可完成（04 若已完成可用來加速新聞策展，但非必要）。

**Blocked by:** 02, 03

**Status:** done

- [x] 2330 2023/04～2023/07 期間的真實 OHLCV 資料已抓取並存入 `/data`（實際交易日 2023-04-06～2023-07-31，共 80 筆，收盤 530→565，+6.6%）
- [x] 該期間 17 則真實新聞已人工蒐集並標註重要度（1～5 星）與是否為關鍵事件
- [x] 已整理出 5 則真正推動這波行情的關鍵新聞，並寫出對應的事件時間軸摘要文字（5 條）
- [x] 前端讀取此真實 scenario，可完整玩過一輪（K 線揭露前 → 選方向 → 選新聞 → 提交 → 揭露走勢／星等／時間軸摘要）
- [x] 這份真實資料存放於 `/data`（以及供前端讀取的 `frontend/public/data/`、pipeline 的原始輸入 `pipeline/scenarios/`），確認三處皆未被提交進 git（受 `.gitignore` 排除，已用 `git check-ignore -v` 驗證）

## Comments

未依賴 04 的 LLM 草稿步驟——新聞內容全部由 Claude Code 親自蒐集/撰寫，因為這是需要對真實歷史事實負責的內容，不適合交給程式碼生成用的 CLI agent。策展依據：先用 WebSearch 確認兩個關鍵事實（NVIDIA 2023/5/24 財報展望是 CNBC 當時報導證實的觸發點；台積電 2023/4/20 法說會其實是保守下修資本支出，並非真正驅動這波漲勢的事件），再用一般性知識補齊其餘 12 則雜訊新聞（Fed 會議、月營收公告等，皆為可查證的公開事實），並選用保守、不誇大具體數字的措辭。

真正跑過完整 pipeline：`pipeline-import-news` → `pipeline-build-scenario`（真實打 yfinance API 抓 2330.TW 股價），產出 `data/tsmc-2023-ai-rally.json`，複製一份到 `frontend/public/data/`，並把 `GameRound.tsx` 指向這個真實 scenario id（原本指向 toy fixture）。額外寫了一個一次性腳本模擬「全對」與「答錯」兩種玩法，確認 `scoreRound` 對真實資料的計分行為符合預期（全對：21/21 且方向正確；答錯：0/21 且方向錯誤），腳本已刪除。`bun run build`、`bun run test`（8/8）、dev server 直接 curl 驗證過 `/data/tsmc-2023-ai-rally.json` 有正確被伺服。

**給未來人工校對的提醒**（呼應 04 的「LLM 草稿 + 人工校對」設計精神——這裡是反過來，一次性人工撰寫，建議之後有機會時比照校對）：新聞的日期與核心事實（NVIDIA 財報日期、台積電法說會日期、Fed 會議日期）皆已查證，但部分雜訊新聞的敘述用詞是基於一般性知識的合理重建，不是逐字引用真實報導標題，正式對外發布前建議再過一輪事實查核。
